%ul#begegnungssearch
  - Category.all.each do |cat|
    %li= link_to simple_format(cat.title.gsub(" ", "<br>")), find_categories_category_abonnements_path(:category => cat.id, :controller_type => @current_controller), :id => "cat-#{cat.id}", :remote => true, :class => "#{(categories[cat.title].to_s == "0") ? 'category_link active' : 'category_link' }" 

  /*= form_tag @current_controller, :method => :get do*/
  /*  = text_field_tag('form-input-location', 'Puch bei Hallein', :class => 'form-input-full')*/
  /*  .submit-field-icon*/
  /*    = submit_tag (@current_controller == 'courses') ? 'Begegnung suchen' : 'Begegnungswunsch suchen', :class => 'form-submit', :id => 'form-submit-search'*/
  /*    %span*/
